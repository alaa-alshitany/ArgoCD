{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "jenkins.fullname" .) }}
  labels:
    {{- include "jenkins.labels" . | nindent 4 }}
{{- end }}